# ============================================
# Financial Intelligence Platform — Backend
# ============================================

# --- Core Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0

# --- Database ---
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0           # Async PostgreSQL driver
alembic==1.13.0            # Database migrations
pgvector==0.3.5            # pgvector support for SQLAlchemy

# --- Cache & Queue ---
redis==5.1.0
celery==5.4.0

# --- HTTP Client ---
httpx==0.27.0              # Async HTTP client (EDGAR, APIs)
aiohttp==3.10.0            # Alternative async HTTP

# --- AI / ML ---
sentence-transformers==3.1.0  # Local embedding model
groq==0.11.0                  # Groq API (free tier extraction)
google-generativeai==0.8.0    # Gemini API
openai==1.50.0                # OpenAI-compatible API
anthropic==0.34.0             # Anthropic API

# --- Data Processing ---
lxml==5.3.0                # XML/HTML parsing (EDGAR filings)
beautifulsoup4==4.12.0     # HTML parsing
python-xbrl==1.1.1         # XBRL parsing
rapidfuzz==3.10.0          # Fuzzy string matching (entity resolution)
pandas==2.2.0              # Data manipulation

# --- Security ---
cryptography==43.0.0       # API key encryption (AES-256-GCM)
python-jose[cryptography]==3.3.0  # JWT handling
passlib[bcrypt]==1.7.4     # Password hashing

# --- Observability ---
loguru==0.7.2              # Structured logging
sentry-sdk[fastapi]==2.14.0  # Error tracking

# --- Utilities ---
python-dotenv==1.0.1       # Environment variables
python-multipart==0.0.9    # File uploads
orjson==3.10.0             # Fast JSON serialization
tenacity==9.0.0            # Retry logic

# --- Testing ---
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.0              # For TestClient

# --- Code Quality ---
ruff==0.6.0                # Linter + formatter
